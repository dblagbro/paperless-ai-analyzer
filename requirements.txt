# Core dependencies
requests==2.31.0
PyYAML==6.0.1
python-dateutil==2.8.2

# PDF and image processing
PyPDF2==3.0.1
pdf2image==1.16.3
Pillow==10.2.0
pypdfium2==4.26.0

# Unstructured for document extraction
# onnxruntime >=1.16.0 required — 1.15.1 crashes with "cannot enable executable stack" on
# Linux kernels with strict NX enforcement (e.g. Ubuntu 22.04+). unstructured-inference
# claims <1.16 but it's just a warning; 1.24.x works fine.
onnxruntime>=1.16.0
unstructured[pdf]==0.12.4
unstructured-inference==0.7.23
pytesseract==0.3.10

# Image forensics
opencv-python==4.9.0.80
numpy==1.26.3
scikit-image==0.22.0
scipy==1.12.0

# Optional LLM support
anthropic>=0.40.0
openai>=1.12.0
cohere>=5.0.0

# Vector database for RAG
chromadb>=0.4.22

# Utilities
tenacity==8.2.3
pydantic==2.6.1

# Web UI
Flask==3.0.0
Werkzeug==3.0.1
waitress==3.0.0  # Production WSGI server
flask-login>=0.6.3
mistune>=3.0.2
weasyprint>=62.0

# Cloud service integrations (v1.5.0)
# Google Drive
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# Dropbox
dropbox>=11.36.0

# Amazon S3
boto3>=1.34.0

# Microsoft OneDrive/Graph API (uses requests, already included)

# Async HTTP for remote downloads
aiohttp>=3.9.0

# Docker SDK for container management (v2.5.0)
docker>=7.0.0

# Court Document Importer (v3.5.0)
cryptography>=42.0.0
playwright>=1.40.0
